# See https://docs.travis-ci.com/user/languages/ruby/
sudo: false
language: ruby
cache: bundler
rvm:
  - 2.4.1
  - 2.1

before_script:
  - cat Gemfile.lock

script:
  - bundle exec rspec --tag ~vagrant
  - bundle exec rubocop

notifications:
  email: false
